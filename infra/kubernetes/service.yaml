apiVersion: v1
kind: Service
metadata:
  name: leaseforge-api-service
  namespace: leaseforge
spec:
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: leaseforge-api
  type: ClusterIP
